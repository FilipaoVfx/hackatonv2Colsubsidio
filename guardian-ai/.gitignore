backend/go.sum
*.log
.env

# compiled Go binary (build artifact, never version)
backend/guardianai

# compiled mock binary
mock-protege/protegemock

# caché de build de Go (los tests corren en contenedor con GOCACHE=/src/.gocache)
backend/.gocache/
